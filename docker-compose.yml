version: "3.7"
services:
    led-a:
       image: "${APPNAME}:${VERSION}"
       deploy:
          replicas: 1
          resources:
             limits:
                memory: "3GB"
       ports:
          - "10004:3838"
       networks:
          - ${APPNAME}
networks:
    led-a:
